# Migration `20191025131857`

This migration has been generated by Andy Kay at 10/25/2019, 1:18:57 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `campus`.`Grade` ADD COLUMN `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
ADD COLUMN `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191025131450..20191025131857
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("MYSQL_URL")
 }
 generator photonjs {
   provider = "photonjs"
@@ -87,5 +87,7 @@
   assignment   Assignment
   letter_grade String
   points       Float
   percent      Float
+  createdAt    DateTime   @default(now())
+  updatedAt    DateTime   @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191025131857)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191025131857'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
