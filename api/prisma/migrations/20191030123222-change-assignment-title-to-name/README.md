# Migration `20191030123222-change-assignment-title-to-name`

This migration has been generated by Andy Kay at 10/30/2019, 12:32:22 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `campus`.`Assignment` DROP COLUMN `title`,
ADD COLUMN `name` varchar(191) NOT NULL DEFAULT '' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191030115012-add-assignments-to-course-and-synapse-instance-id..20191030123222-change-assignment-title-to-name
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("MYSQL_URL")
 }
 generator photonjs {
   provider = "photonjs"
@@ -74,9 +74,9 @@
 model Assignment {
   id        String   @id @default(cuid())
   course    Course
-  title     String
+  name      String
   points    Int
   grades    Grade[]
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
```

## Photon Usage

You can use a specific Photon built for this migration (20191030123222-change-assignment-title-to-name)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191030123222-change-assignment-title-to-name'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
