# Migration `20190813093156-courses`

This migration has been generated by Andy Kay at 8/13/2019, 9:31:57 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `better`.`Course`(`id` varchar(767) NOT NULL  ,`name` varchar(767) NOT NULL DEFAULT '' ,`title` varchar(767)   ,`createdAt` datetime(3) NOT NULL  ,`updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,PRIMARY KEY (`id`));

CREATE TABLE `better`.`_CourseToUser`(`A` varchar(767)  , FOREIGN KEY (`A`) REFERENCES `better`.`Course`(`id`) ON DELETE CASCADE,`B` varchar(767)  , FOREIGN KEY (`B`) REFERENCES `better`.`User`(`id`) ON DELETE CASCADE);
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190805101709-init..20190813093156-courses
--- datamodel.dml
+++ datamodel.dml
@@ -14,7 +14,17 @@
 model User {
   id        String   @id @default(cuid())
   name      String
   email     String   @unique
+  courses   Course[]
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
+}
+
+model Course {
+  id        String   @id @default(cuid())
+  name      String
+  title     String?
+  users     User[]
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190813093156-courses)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190813093156-courses'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
