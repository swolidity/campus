# Migration `20191030115012-add-assignments-to-course-and-synapse-instance-id`

This migration has been generated by Andy Kay at 10/30/2019, 11:50:12 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191025131857..20191030115012-add-assignments-to-course-and-synapse-instance-id
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url      = "***"
+  url      = env("MYSQL_URL")
 }
 generator photonjs {
   provider = "photonjs"
@@ -18,24 +18,26 @@
   updatedAt DateTime @updatedAt
 }
 model Course {
-  id             String          @id @default(cuid())
-  parent         String?
-  name           String
-  title          String?
-  description    String?
-  term           Int
-  slug           String?         @unique
-  subject        String
-  catalog_number Int
-  component      String
-  class_number   Int             @unique
-  users          User[]
-  content        Content[]
-  messages       CourseMessage[]
-  createdAt      DateTime        @default(now())
-  updatedAt      DateTime        @updatedAt
+  id                  String          @id @default(cuid())
+  parent              String?
+  name                String
+  title               String?
+  description         String?
+  term                Int
+  slug                String?         @unique
+  subject             String
+  catalog_number      Int
+  component           String
+  class_number        Int             @unique
+  users               User[]
+  content             Content[]
+  messages            CourseMessage[]
+  assignments         Assignment[]
+  synapse_instance_id Int
+  createdAt           DateTime        @default(now())
+  updatedAt           DateTime        @updatedAt
 }
 model CourseMessage {
   id        String   @id @default(cuid())
```

## Photon Usage

You can use a specific Photon built for this migration (20191030115012-add-assignments-to-course-and-synapse-instance-id)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191030115012-add-assignments-to-course-and-synapse-instance-id'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
