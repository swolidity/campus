# Migration `20191022111358-remove-course_messages-relation-on-user`

This migration has been generated by Andy Kay at 10/22/2019, 11:13:58 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE UNIQUE INDEX `_CourseToUser_AB_unique` ON `campus`.`_CourseToUser`(`A`,`B`)
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191022111102-make-course-message-user-relation-a-1-to-many..20191022111358-remove-course_messages-relation-on-user
--- datamodel.dml
+++ datamodel.dml
@@ -11,17 +11,16 @@
   provider = "nexus-prisma"
 }
 model User {
-  id              String          @id @default(cuid())
-  name            String
-  email           String          @unique
-  picture         String?
-  courses         Course[]
-  course_messages CourseMessage[]
-  role            Role            @default(STUDENT)
-  createdAt       DateTime        @default(now())
-  updatedAt       DateTime        @updatedAt
+  id        String   @id @default(cuid())
+  name      String
+  email     String   @unique
+  picture   String?
+  courses   Course[]
+  role      Role     @default(STUDENT)
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
 }
 model Course {
   id             String          @id @default(cuid())
```

## Photon Usage

You can use a specific Photon built for this migration (20191022111358-remove-course_messages-relation-on-user)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191022111358-remove-course_messages-relation-on-user'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
