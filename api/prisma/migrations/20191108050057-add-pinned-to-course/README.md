# Migration `20191108050057-add-pinned-to-course`

This migration has been generated by Andy Kay at 11/8/2019, 5:00:57 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `campus`.`Course` ADD COLUMN `pinned` int   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191102090833-make-assignment-slug-unique..20191108050057-add-pinned-to-course
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("MYSQL_URL")
 }
 generator photonjs {
   provider = "photonjs"
@@ -34,8 +34,9 @@
   content             Content[]
   messages            CourseMessage[]
   assignments         Assignment[]
   synapse_instance_id Int
+  pinned              Int?
   createdAt           DateTime        @default(now())
   updatedAt           DateTime        @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191108050057-add-pinned-to-course)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191108050057-add-pinned-to-course'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
