# Migration `20191031094949-add-assignment-slug`

This migration has been generated by Andy Kay at 10/31/2019, 9:49:49 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `campus`.`Assignment` ADD COLUMN `slug` varchar(191) NOT NULL DEFAULT '' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191030123222-change-assignment-title-to-name..20191031094949-add-assignment-slug
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("MYSQL_URL")
 }
 generator photonjs {
   provider = "photonjs"
@@ -73,8 +73,9 @@
 }
 model Assignment {
   id        String   @id @default(cuid())
+  slug      String
   course    Course
   name      String
   points    Int
   grades    Grade[]
```

## Photon Usage

You can use a specific Photon built for this migration (20191031094949-add-assignment-slug)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191031094949-add-assignment-slug'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
