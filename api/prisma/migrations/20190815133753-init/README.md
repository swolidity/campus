# Migration `20190815133753-init`

This migration has been generated by Andy Kay at 8/15/2019, 1:37:53 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `better`.`_CourseToUser` DROP COLUMN `A`,ADD COLUMN `A` varchar(191)  ,ADD FOREIGN KEY (`A`) REFERENCES `better`.`Course`(`id`) ON DELETE CASCADE,DROP COLUMN `B`,ADD COLUMN `B` varchar(191)  ,ADD FOREIGN KEY (`B`) REFERENCES `better`.`User`(`id`) ON DELETE CASCADE;

CREATE UNIQUE INDEX `User.email._UNIQUE` ON `better`.`User`(`email`)

CREATE UNIQUE INDEX `Course.class_number._UNIQUE` ON `better`.`Course`(`class_number`)
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190815133753-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,35 @@
+datasource db {
+  provider = "mysql"
+  url      = "mysql://root:root@localhost:3306/better"
+}
+
+generator photonjs {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
+model User {
+  id        String   @id @default(cuid())
+  name      String
+  email     String   @unique
+  courses   Course[]
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+model Course {
+  id             String   @id @default(cuid())
+  name           String
+  title          String?
+  term           Int
+  subject        String
+  catalog_number Int
+  component      String
+  class_number   Int      @unique
+  users          User[]
+  createdAt      DateTime @default(now())
+  updatedAt      DateTime @updatedAt
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190815133753-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190815133753-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
