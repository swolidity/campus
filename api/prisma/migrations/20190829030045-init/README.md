# Migration `20190829030045-init`

This migration has been generated by Andy Kay at 8/29/2019, 3:00:45 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `better`.`User`(`id` varchar(191) NOT NULL  ,`name` varchar(191) NOT NULL DEFAULT '' ,`email` varchar(191) NOT NULL DEFAULT '' ,`createdAt` datetime(3) NOT NULL  ,`updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,PRIMARY KEY (`id`))
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `better`.`Course`(`id` varchar(191) NOT NULL  ,`name` varchar(191) NOT NULL DEFAULT '' ,`title` varchar(191)   ,`term` int NOT NULL DEFAULT 0 ,`subject` varchar(191) NOT NULL DEFAULT '' ,`catalog_number` int NOT NULL DEFAULT 0 ,`component` varchar(191) NOT NULL DEFAULT '' ,`class_number` int NOT NULL DEFAULT 0 ,`createdAt` datetime(3) NOT NULL  ,`updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,PRIMARY KEY (`id`))
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `better`.`CourseMessage`(`id` varchar(191) NOT NULL  ,`message` varchar(191) NOT NULL DEFAULT '' ,`createdAt` datetime(3) NOT NULL  ,`updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,PRIMARY KEY (`id`))
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `better`.`_CourseToUser`(`A` varchar(191)  , FOREIGN KEY (`A`) REFERENCES `better`.`Course`(`id`) ON DELETE CASCADE,`B` varchar(191)  , FOREIGN KEY (`B`) REFERENCES `better`.`User`(`id`) ON DELETE CASCADE)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `better`.`_CourseMessageToUser`(`A` varchar(191)  , FOREIGN KEY (`A`) REFERENCES `better`.`CourseMessage`(`id`) ON DELETE CASCADE,`B` varchar(191)  , FOREIGN KEY (`B`) REFERENCES `better`.`User`(`id`) ON DELETE CASCADE)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

ALTER TABLE `better`.`CourseMessage` ADD COLUMN `course` varchar(191)  ,ADD FOREIGN KEY (`course`) REFERENCES `better`.`Course`(`id`) ON DELETE SET NULL;

CREATE UNIQUE INDEX `User.email._UNIQUE` ON `better`.`User`(`email`)

CREATE UNIQUE INDEX `Course.class_number._UNIQUE` ON `better`.`Course`(`class_number`)
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190829030045-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,46 @@
+datasource db {
+  provider = "mysql"
+  url      = env("MYSQL_URL")
+}
+
+generator photonjs {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
+model User {
+  id              String          @id @default(cuid())
+  name            String
+  email           String          @unique
+  courses         Course[]
+  course_messages CourseMessage[]
+  createdAt       DateTime        @default(now())
+  updatedAt       DateTime        @updatedAt
+}
+
+model Course {
+  id             String          @id @default(cuid())
+  name           String
+  title          String?
+  term           Int
+  subject        String
+  catalog_number Int
+  component      String
+  class_number   Int             @unique
+  users          User[]
+  messages       CourseMessage[]
+  createdAt      DateTime        @default(now())
+  updatedAt      DateTime        @updatedAt
+}
+
+model CourseMessage {
+  id        String   @id @default(cuid())
+  course    Course
+  user      User[]
+  message   String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190829030045-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190829030045-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
