# Migration `20191025124600`

This migration has been generated by Andy Kay at 10/25/2019, 12:46:00 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191025111314..20191025124600
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("MYSQL_URL")
 }
 generator photonjs {
   provider = "photonjs"
@@ -49,5 +49,40 @@
   ADMIN
   PROFESSOR
   TEACHING_ASSISSTANT
   STUDENT
+}
+
+model Content {
+  id     String      @id @default(cuid())
+  course Course
+  title  String
+  type   ContentType
+  url    String?
+  text   String?
+}
+
+enum ContentType {
+  TEXT
+  FILE
+  UNIT
+}
+
+model Assignment {
+  id        String   @id @default(cuid())
+  course    Course
+  title     String
+  points    Int
+  grades    Grade[]
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+model Grade {
+  id           String     @id @default(cuid())
+  user         User
+  course       Course
+  assignment   Assignment
+  letter_grade String
+  points       Float
+  percent      Float
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191025124600)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191025124600'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
