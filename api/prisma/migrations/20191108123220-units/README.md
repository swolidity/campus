# Migration `20191108123220-units`

This migration has been generated by Andy Kay at 11/8/2019, 12:32:20 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `campus`.`Unit` (
  `course` varchar(191)  ,
 FOREIGN KEY (`course`) REFERENCES `campus`.`Course`(`id`) ON DELETE SET NULL,
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `description` varchar(191) NOT NULL DEFAULT '' ,
  `id` varchar(191) NOT NULL  ,
  `name` varchar(191) NOT NULL DEFAULT '' ,
  `order` int NOT NULL DEFAULT 0 ,
  `slug` varchar(191) NOT NULL DEFAULT '' ,
  `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `visible` boolean NOT NULL DEFAULT false ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE UNIQUE INDEX `Unit.slug` ON `campus`.`Unit`(`slug`)
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191108050057-add-pinned-to-course..20191108123220-units
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("MYSQL_URL")
 }
 generator photonjs {
   provider = "photonjs"
@@ -30,8 +30,9 @@
   catalog_number      Int
   component           String
   class_number        Int             @unique
   users               User[]
+  units               Unit[]
   content             Content[]
   messages            CourseMessage[]
   assignments         Assignment[]
   synapse_instance_id Int
@@ -55,8 +56,20 @@
   TEACHING_ASSISSTANT
   STUDENT
 }
+model Unit {
+  id          String   @id @default(cuid())
+  course      Course
+  name        String
+  slug        String   @unique
+  description String
+  order       Int
+  visible     Boolean  @default(false)
+  createdAt   DateTime @default(now())
+  updatedAt   DateTime @updatedAt
+}
+
 model Content {
   id        String      @id @default(cuid())
   course    Course
   title     String
```

## Photon Usage

You can use a specific Photon built for this migration (20191108123220-units)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191108123220-units'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
