# Migration `20191025131450`

This migration has been generated by Andy Kay at 10/25/2019, 1:14:50 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `campus`.`Content` ADD COLUMN `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
ADD COLUMN `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191025125943..20191025131450
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("MYSQL_URL")
 }
 generator photonjs {
   provider = "photonjs"
@@ -53,14 +53,16 @@
   STUDENT
 }
 model Content {
-  id     String      @id @default(cuid())
-  course Course
-  title  String
-  type   ContentType
-  url    String?
-  text   String?
+  id        String      @id @default(cuid())
+  course    Course
+  title     String
+  type      ContentType
+  url       String?
+  text      String?
+  createdAt DateTime    @default(now())
+  updatedAt DateTime    @updatedAt
 }
 enum ContentType {
   TEXT
```

## Photon Usage

You can use a specific Photon built for this migration (20191025131450)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191025131450'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
