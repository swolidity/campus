# Migration `20190805101709-init`

This migration has been generated by Andy Kay at 8/5/2019, 10:17:09 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `better`.`User`(`id` varchar(1000) NOT NULL  ,`name` varchar(1000) NOT NULL DEFAULT '' ,`email` varchar(1000) NOT NULL DEFAULT '' ,`createdAt` datetime(3) NOT NULL  ,`updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,PRIMARY KEY (`id`));

CREATE UNIQUE INDEX `User.email._UNIQUE` ON `better`.`User`(`email`)
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190805101709-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,20 @@
+datasource db {
+  provider = "mysql"
+  url      = "mysql://root:root@localhost:3306/better"
+}
+
+generator photonjs {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
+model User {
+  id        String   @id @default(cuid())
+  name      String
+  email     String   @unique
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190805101709-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190805101709-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
