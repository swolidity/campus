# Migration `20190828161611-course-message`

This migration has been generated by Andy Kay at 8/28/2019, 4:16:11 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `better`.`CourseMessage`(`id` varchar(191) NOT NULL  ,`message` varchar(191) NOT NULL DEFAULT '' ,`createdAt` datetime(3) NOT NULL  ,`updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,`user` varchar(191)  , FOREIGN KEY (`user`) REFERENCES `better`.`User`(`id`) ON DELETE SET NULL,PRIMARY KEY (`id`))
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

ALTER TABLE `better`.`Course` ADD COLUMN `courseMessage` varchar(191)  ,ADD FOREIGN KEY (`courseMessage`) REFERENCES `better`.`CourseMessage`(`id`) ON DELETE SET NULL;

ALTER TABLE `better`.`_CourseToUser` DROP COLUMN `A`,ADD COLUMN `A` varchar(191)  ,ADD FOREIGN KEY (`A`) REFERENCES `better`.`Course`(`id`) ON DELETE CASCADE,DROP COLUMN `B`,ADD COLUMN `B` varchar(191)  ,ADD FOREIGN KEY (`B`) REFERENCES `better`.`User`(`id`) ON DELETE CASCADE;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190815133753-init..20190828161611-course-message
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url      = "mysql://root:root@localhost:3306/better"
+  url      = env("MYSQL_URL")
 }
 generator photonjs {
   provider = "photonjs"
@@ -31,5 +31,14 @@
   class_number   Int      @unique
   users          User[]
   createdAt      DateTime @default(now())
   updatedAt      DateTime @updatedAt
+}
+
+model CourseMessage {
+  id        String   @id @default(cuid())
+  course    Course
+  user      User
+  message   String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190828161611-course-message)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190828161611-course-message'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
